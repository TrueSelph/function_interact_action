package:
  name: jivas/function_interact_action
  author: V75 Inc.
  archetype: FunctionInteractAction
  version: 0.1.0
  meta:
    title: Function Interact Action
    description: Chooses the best action(s) (intents) and extracts any related params.
    group: core
    type: interact_action
  config:
    singleton: true
    order:
      weight: 0
      before: all
  dependencies:
    jivas: ~2.1.0
    actions:
      jivas/langchain_model_action: ~0.1.0


